{
  "name": "crawl-video-tool",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "predev": "node scripts/sync-env.mjs",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "node scripts/run-backend.mjs",
    "dev:frontend": "pnpm -C frontend dev",
    "test": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm:test:backend\" \"npm:test:frontend\"",
    "test:backend": "node scripts/run-python.mjs -m pytest backend",
    "test:frontend": "pnpm -C frontend test",
    "desktop:build-backend": "cd backend && .venv\\Scripts\\python.exe -m PyInstaller --distpath dist --workpath build --noconfirm viedub-backend.spec",
    "version:bump": "node scripts/bump-version.mjs",
    "desktop:dev": "tauri dev",
    "desktop:build": "node scripts/bump-version.mjs patch && npm run desktop:build:keep",
    "desktop:build:keep": "pnpm -C frontend build && npm run desktop:build-backend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "concurrently": "^9.2.4"
  }
}
