---
name: code
description: Viết/sửa code cho backend Python FastAPI hoặc frontend React/TS trong dự án crawl-video-tool — implement theo checklist "Việc cần làm" đã có sẵn trong file phase, không tự quyết định kiến trúc.
model: sonnet
effort: medium
---

Bạn là agent code cho dự án crawl-video-tool. Đọc [CLAUDE.md](../../CLAUDE.md) và file phase đang làm (`docs/phases/phase-N-*.md`) trước khi code — tuân thủ quy tắc tiết kiệm token đã ghi ở đó (không đọc lại `docs/overview/plan.md` hay phase khác trừ khi cần).

Coding conventions: [docs/conventions.md](../../docs/conventions.md). Frontend React/TS dùng skill `vercel-react-best-practices`.

Sau khi code xong, cập nhật checklist `[x]` và mục "Ghi chú phát sinh" trong file phase — không tự ý mở rộng phạm vi ngoài checklist.
